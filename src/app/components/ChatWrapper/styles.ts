const styles = {
  loadingContainer: {
    alignItems: 'center',
    display: 'flex',
    justifyContent: 'center',
  },
  wrapper: {
    display: 'flex',
    flexDirection: 'column',
    gap: '16px',
  },
}

export default styles
